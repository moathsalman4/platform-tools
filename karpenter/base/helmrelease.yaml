apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: karpenter
  namespace: karpenter
spec:
  interval: 10m
  chart:
    spec:
      chart: karpenter
      version: "1.3.3"
      sourceRef:
        kind: HelmRepository
        name: karpenter
        namespace: flux-system
  install:
    createNamespace: false
    crds: Create
  upgrade:
    crds: CreateReplace
  values:
    serviceAccount:
      annotations:
        eks.amazonaws.com/role-arn: arn:aws:iam::665832051028:role/karpenter_irsa_role
    settings:
      clusterName: projectx_cluster_ubuntu25b
      clusterEndpoint: https://D61E52E76FBF3DDE20ED109C2F383FF4.gr7.us-east-1.eks.amazonaws.com
      interruptionQueue: projectx_cluster_ubuntu25b